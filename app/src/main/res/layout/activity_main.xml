<?xml version="1.0" encoding="utf-8"?>

<LinearLayout

    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:background="@color/colobackground"
    tools:context="com.modulo.tp1.reproductor_v01.MainActivity">

    <ListView
        android:id="@+id/song_list"
        android:layout_width="fill_parent"
        android:layout_height="289dp" >
    </ListView>
    <EditText
        android:id="@+id/etTextHint"
        android:gravity="top"
        android:inputType="textMultiLine"
        android:lines="1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="etSearchHint" />

    <Button
        android:id="@+id/btSpeak"
        android:onClick="speak"
        android:padding="8dp"
        android:text="btSpeak"
        tools:context=".VoiceRecognitionActivity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />


    <TextView
        android:text="tvTextMatches"
        android:textStyle="bold"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <ListView
        android:id="@+id/lvTextMatches"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</LinearLayout>
